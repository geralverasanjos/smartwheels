rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Regra geral para permitir acesso a utilizadores autenticados
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
